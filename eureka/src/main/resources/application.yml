#spring.application.name=eureka
spring:
  application:
    name: eureka-server

server:
  port: 8761

eureka:
  client:
    register-with-eureka: true # ?? ??? ?? ??? ???? ??
    fetch-registry: true       # ??? ?? ????? ??? ???? ??
  server:
    enable-self-preservation: false # ??? ???? ?? ?? ????
